---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: customscalers.autoscaling.my.domain
spec:
  group: autoscaling.my.domain
  names:
    kind: CustomScaler
    listKind: CustomScalerList
    plural: customscalers
    singular: customscaler
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CustomScaler is the Schema for the customscalers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CustomScaler
            properties:
              deploymentName:
                description: Name of the Deployment to scale
                type: string
              url:
                description: The external endpoint to poll (e.g., http://localhost:1208)
                type: string
            required:
            - deploymentName
            - url
            type: object
          status:
            description: status defines the observed state of CustomScaler
            properties:
              currentReplicas:
                description: Current replica count
                format: int32
                type: integer
              lastValue:
                description: The last value received from the endpoint
                type: integer
            required:
            - currentReplicas
            - lastValue
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
